fix(compliance): correct timezone and exists() bug in enforceInactivityTimeout

## Issues Fixed

### 1. Timezone Mismatch (Bug)
- Problem: Line 802 used UTC date (toISOString) but date keys should be EST/EDT
- Impact: Sessions spanning midnight EST/EDT would create mismatched daily_activity_logs keys
- Result: Idle time deductions would fail or affect wrong day
- Fix: Use same EST timezone conversion as rest of codebase (lines 162-169)

### 2. Method Call Bug (Critical)
- Problem: Line 806 used dailyLogDoc.exists (property access) instead of dailyLogDoc.exists()
- Impact: Condition always truthy since .exists is a function; logic incorrect
- Fix: Added parentheses to properly invoke Firestore method: dailyLogDoc.exists()

## Changes
- Extract EST date conversion (lines 802-806)
- Use consistent dateKey format: YYYY-MM-DD
- Fix .exists() method call (line 811)
- Matches existing timezone pattern in sessionHeartbeat section

## Build Status
✅ Frontend build successful
✅ No errors or warnings
